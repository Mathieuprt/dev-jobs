{% extends 'base.html.twig' %}

{% block title %}Création société{% endblock %}

{% block body %}

    <section class="banner banner__candidat">
        <div class="container">
            <h1 class="heading heading--1 heading--1__white">Créer ma société</h1>
        </div>
    </section>

    <main>
        <div class="container">
            <section class="page__content--form">
                <h2 class="heading heading--h2 screen-reader-text">Page création de société</h2>
                <p>Remplissez le formulaire ci-contre pour créer pour société</p>

                <div class="form__societe">
                    {{ form_start(societe_form) }}
                    {{ form_widget(societe_form) }}
                    <button class="button button--primary">{{ button_label|default('Créer') }}</button>
                    {{ form_end(societe_form) }}
                </div>
            </section>
        </div>
    </main>

{% endblock %}